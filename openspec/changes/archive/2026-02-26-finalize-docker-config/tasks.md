## 1. Docker Dev Configuration

- [x] 1.1 Review and adjust dev compose to ensure API + MariaDB + Redis run together
- [x] 1.2 Document dev compose usage and required env vars

## 2. Docker Prod Configuration

- [x] 2.1 Update prod compose to run API container only
- [x] 2.2 Ensure prod compose relies on DB*\* and REDIS*\* env vars

## 3. Tests

- [x] 3.1 Add configuration validation tests for prod env requirements
